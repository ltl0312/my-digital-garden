---
aliases: [Nginx 代理配置, Nginx upstream 配置, Nginx CORS 配置]
maturity: SEEDLING
tags:
  - status/进行中
  - type/教程
  - 运维/Nginx
created: "2026-06-29 15:10"
updated: "2026-06-29 15:10"
source: "https://nginx.org/en/docs/http/ngx_http_proxy_module.html"
知识体系: "DevOps/运维 → L3 编排与网关"
技术版本: "Nginx 1.24.x"
---

# Nginx — 反向代理与负载均衡配置指南

> 基础安装请参考 <a href="obsidian://open?file=环境配置与项目初始化">Nginx 环境配置与安装</a>。

## Step 1: 反向代理基础模板

```nginx
server {
    listen 80;
    server_name example.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## Step 2: 负载均衡配置

```nginx
upstream backend {
    # 轮询（默认）
    server 192.168.1.101:8080;
    server 192.168.1.102:8080;

    # 权重
    # server 192.168.1.101:8080 weight=3;
    # server 192.168.1.102:8080 weight=1;

    # IP 哈希（会话保持）
    # ip_hash;
}

server {
    location / {
        proxy_pass http://backend;
    }
}
```

## Step 3: 跨域解决方案

```nginx
location /api/ {
    proxy_pass http://backend:8080/;
    add_header Access-Control-Allow-Origin $http_origin;
    add_header Access-Control-Allow-Methods "GET,POST,PUT,DELETE,OPTIONS";
    add_header Access-Control-Allow-Headers "Content-Type,Authorization";
    add_header Access-Control-Allow-Credentials true;
}
```

## Step 4: 多服务路径转发

```nginx
location /user/  { proxy_pass http://127.0.0.1:5001/; }
location /order/ { proxy_pass http://127.0.0.1:5002/; }
location /pay/   { proxy_pass http://127.0.0.1:5003/; }
```

## Step 5: proxy_pass 末尾斜杠规则

| `proxy_pass` | 请求 `/api/user` | 转发目标 |
|-------------|-----------------|---------|
| `http://backend:8080/`（有 `/`） | `/api/user` | `http://backend:8080/user` |
| `http://backend:8080`（无 `/`） | `/api/user` | `http://backend:8080/api/user` |

> 📂 所属：[[MOC - Nginx]]
