---
aliases: [TS 泛型调试, 泛型约束配置]
tags: [status/进行中, type/教程, 前端/TypeScript]
created: "2026-06-16 19:28"
updated: "2026-06-16 19:28"
source: "https://www.typescriptlang.org/docs/handbook/2/generics.html"
知识体系: "前端 → L3 工程化"
技术版本: "TypeScript 5.x"
---

# TypeScript 泛型 — 环境配置

## 前置条件
- TS 开发环境（参考 <a href="obsidian://open?file=基础语法-环境配置与项目初始化">TypeScript-基础语法-环境配置与项目初始化</a>）

## Step 1: 泛型推导验证

```typescript
// TS Playground 中悬停查看类型推导
const identity = <T>(arg: T): T => arg;
const a = identity("hello");   // 悬停看 T 推导为 "hello" (字面量) 或 string
const b = identity(42);        // T = 42 | number

// 泛型约束测试
const getLen = <T extends { length: number }>(v: T) => v.length;
getLen("abc");  // 3
// getLen(123); // ❌ 编译错误
```

## Step 2: 常用泛型模式

```typescript
// API 响应包装
interface ApiResponse<T> {
    data: T;
    status: number;
    message: string;
}
type UserResponse = ApiResponse<{ id: number; name: string }>;
```

## 常见问题

1. **泛型推导过于宽泛**：`identity("hello")` 推导 T 为 `string` 而非 `"hello"`。加 `as const` 或显式指定 `<"hello">`。
2. **循环泛型约束**：`<T extends U, U extends T>` 不会报循环错误，TS 有上限检查。

> 📂 所属：[[MOC - TypeScript]]
