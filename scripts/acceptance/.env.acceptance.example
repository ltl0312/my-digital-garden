# 验收脚本本地配置模板
# 复制为仓库根目录的 `.env.acceptance` 并填入真实值：
#   cp scripts/acceptance/.env.acceptance.example .env.acceptance
# `.env.acceptance` 已被 .gitignore（.env.*）覆盖，不会进入仓库。

# 初始管理员（root）的访问密钥 —— 真实凭据，绝不入库
GARDEN_ROOT_KEY=在这里填入root密钥

# 被测应用基地址（默认 http://localhost:3100，可省略）
# GARDEN_BASE=http://localhost:3100

# Chrome 可执行文件路径（默认 C:/Program Files/Google/Chrome/Application/chrome.exe，可省略）
# GARDEN_CHROME=C:/Program Files/Google/Chrome/Application/chrome.exe

# puppeteer-core 所在目录（项目内已安装时可省略）
# PUPPETEER_CORE_DIR=
